interface:
  display_name: "Platform Admin Prototype"
  short_description: "Plan, build, review, and annotate PC admin prototypes"
  brand_color: "#165DFF"
  default_prompt: "Use $platform-admin-prototype to plan, build, review, or produce an annotated PC backend/admin prototype using the local customized platform components."
policy:
  allow_implicit_invocation: false
