{
  "name": "@platform/playground",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vue-tsc -p tsconfig.json --noEmit && vite build",
    "test": "vitest run",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@arco-design/web-vue": "^2.0.0",
    "@platform/components": "workspace:*",
    "@platform/prd-composer": "workspace:*",
    "@platform/registry": "workspace:*",
    "@platform/tokens": "workspace:*",
    "vue": "^3.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0",
    "vue-tsc": "^2.0.0"
  }
}
